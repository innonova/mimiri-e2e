# Copy to `.env` and adjust as needed.

# Which artifact version under `artifacts/<version>` to launch.
APP_VERSION=current

# Linux package format to test: targz | flatpak | appimage (default targz).
APP_FORMAT=targz

# Enables the app's test-mode seam (see helpers/app.ts).
APP_TEST_MODE=1
